pipeline {
    agent any
    parameters {
      choice(name: 'test', choices: "green\nyellow", description: 'zzz' )
    }
      stages {
        stage('build') {
          steps {
            echo "${test}"
            }
        }
    }
}
